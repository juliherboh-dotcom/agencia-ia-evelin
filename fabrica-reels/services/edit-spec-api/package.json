{
  "name": "edit-spec-api",
  "version": "1.0.0",
  "private": true,
  "description": "Servicio HTTP delgado que expone validateEditSpec(), buildRepairPrompt() y los prompts del Edit Director para que n8n (Capa 3) los use sin duplicar lógica de TypeScript dentro de nodos Code.",
  "scripts": {
    "start": "ts-node --transpile-only src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "express": "^4.19.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "ts-node": "^10.9.2",
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.2"
  }
}
